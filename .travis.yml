language: julia
os:
  - linux
#   - osx
julia:
  - 0.7
notifications:
  email: false
sudo: true
before_install:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
script:
  - julia --check-bounds=yes -e 'using Pkg; Pkg.clone(pwd()); Pkg.build("EEG"); Pkg.test("EEG"; coverage=true)'
after_success:
  - julia -e 'using Pkg; cd(Pkg.dir("EEG")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(process_folder()); Codecov.submit(process_folder())'
  - julia -e 'using Pkg; cd(Pkg.dir("EEG")); Pkg.add("Coverage"); using Coverage; Codecov.submit(Codecov.process_folder())'
  - julia -e 'using Pkg; Pkg.add("Documenter")'
  - julia -e 'using Pkg; cd(Pkg.dir("EEG")); include(joinpath("docs", "make.jl"))'
